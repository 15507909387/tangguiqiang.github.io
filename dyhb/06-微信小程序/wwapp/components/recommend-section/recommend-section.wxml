<!--components/recommend-section/recommend-section.wxml-->
<wxs src="../../utils/looks.wxs" module="looks"></wxs>
<view class="title">{{item.title}}</view>
<view class="list" style="margin: 35rpx;">
  <book-item  wx:for="{{looks.slice(item.books, 0, 5)}}" wx:key="index" wx:for-item="book" book="{{book}}"></book-item>
  <!-- <book-item wx:for="{{item.books}}" wx:key="index" book="{{item}}"></book-item> -->
  <!-- <book-item wx:for="{{looks.slice(item.books, 0, 5)}}" wx:key="index" book="{{item}}"></book-item> -->
</view>
